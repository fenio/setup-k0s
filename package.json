{
  "name": "setup-k0s",
  "version": "1.0.0",
  "description": "GitHub Action to install and configure k0s with automatic cleanup",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "format": "prettier --write 'src/**/*.ts'",
    "lint": "eslint src/**/*.ts",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fenio/setup-k0s.git"
  },
  "keywords": [
    "actions",
    "kubernetes",
    "k0s",
    "ci",
    "cd"
  ],
  "author": "fenio",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}
